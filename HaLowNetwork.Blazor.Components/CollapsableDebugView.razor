<div class="card">
    <div class="card-header">
        <h2 class="mb-0">
            <button class="btn btn-link" type="button" @onclick="Toggle">
                @Title
            </button>
        </h2>
    </div>
    @if (_show)
    {
        <div id="collapseOne" class="collapse show">
            <div class="card-body">
                @ChildContent
            </div>
        </div>
    }
</div>

@code{

    [Parameter]
    public RenderFragment ChildContent { get; set; }

    [Parameter]
    public string Title { get; set; }

    private bool _show = false;

    private void Toggle()
    {
        _show = !_show;
        StateHasChanged();
    }

}